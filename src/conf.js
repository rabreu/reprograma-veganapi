const PORT = 8080
const DATABASE_HOSTNAME = "localhost"
const DATABASE_PORT = 27017
const DATABASE_NAME = "VeganAPI"

module.exports = {
    PORT,
    DATABASE_HOSTNAME,
    DATABASE_PORT,
    DATABASE_NAME
}